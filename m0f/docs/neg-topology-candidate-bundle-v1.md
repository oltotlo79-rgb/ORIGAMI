# NEG-TOPOLOGY exact-negative candidate bundle v1

Status: noncanonical candidate parser-regression evidence.

## Boundary

This bundle has exactly one detector boundary: `parseArtifactContractV1` in `m0f/artifacts/contract.ts`. A successful verification means the two saved project-authored controls are freshly accepted and each saved negative source is freshly rejected with the ledger's complete ordered `{path, code}` array.

This result concerns the current artifact-contract topology parser only. It is not planarization or reconstruction evidence, a scientific topology proof, general hole recognition/support, or a complete manifoldness theorem. It does not establish crease-pattern validity or constructibility, MV validity, foldability, collision freedom, a SupportProfile, a ToleranceProfile, scientific verification, canonical promotion, or M0F GO.

## Saved controls

The payload includes two hash-bound accepted controls rather than relying on a mutable external fixture:

1. `control-primary-design.json` is the self-contained project-authored design artifact equivalent to `tests/vectors/m0f-0/artifact-contract-design-v1.json` at generator authoring time.
2. `control-filled-annulus-fold.json` is a project-authored FOLD V8/E12/F5 connected disk. Four annular quadrilaterals surround one center face. Its interior rows are ordered spokes first, then inner-square edges.

Each negative artifact's fixed provenance depends on the corresponding saved control artifact. Verification uses those saved parsed bytes as the difference anchor and separately requires exact deterministic regeneration.

## Fixed negative cases

The closed ordered case list is:

- `NEG-TOPOLOGY-UNSPLIT-CROSSING`
- `NEG-TOPOLOGY-DUPLICATE-EDGE`
- `NEG-TOPOLOGY-ZERO-AREA-FACE`
- `NEG-TOPOLOGY-HOLE`
- `NEG-TOPOLOGY-NON-MANIFOLD-EDGE`

The hole case coherently removes the center face, changes the inner-square edges to boundary edges in both FOLD input and mesh, and removes dependent target/path rows. It remains a connected annulus and isolates the current disk Euler rejection. That case is not a claim that every hole or unsupported topology is recognized.

## Closed ledger and byte contract

`neg-topology-candidate-bundle-ledger-v1.schema.json` is Draft 2020-12. Both the strict schema and runtime parser fix:

- five case rows, in order, including control link, mutation, complete ordered changed paths, source path, and complete ordered parser issue oracle;
- eight payload artifact rows, in order: README, two controls, and five negative sources;
- artifact identity, role, path, media type, lowercase SHA-256, MIT license, and exact provenance dependency;
- candidate-only and false claim fields.

The directory has exactly nine regular files after including the ledger. Writers refuse unexpected entries instead of deleting them. Verification rejects unreadable/linked/extra entries, hash drift, invalid saved controls, control/source delta drift, parser-oracle drift, canonical registration, and regenerated byte drift.

Serialization is stable JSON UTF-8 with one LF. This supports deterministic local replay; it does not supply signatures or TOCTOU protection.

## Commands

```text
npx tsx m0f/neg-topology-candidate-bundle-cli.ts --verify
npx tsx m0f/neg-topology-candidate-bundle-cli.ts --write
npx tsx m0f/neg-topology-candidate-bundle-cli.ts --verify --json
```
