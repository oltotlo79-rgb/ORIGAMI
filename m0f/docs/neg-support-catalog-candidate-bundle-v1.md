# NEG-SUPPORT-CATALOG exact-negative candidate bundle v1

Status: noncanonical candidate-catalog parser-regression evidence.

## Boundary

This bundle has exactly one detector boundary: `parseSupportProfileCandidatesV1` in `m0f/profiles/support-profiles.ts`. Verification means the saved project-authored candidate catalog is freshly accepted and every saved negative source is freshly rejected with the ledger's complete ordered `{path, code}` array.

The `NEG-SUPPORT-CATALOG-*` IDs are deliberately distinct from canonical `NEG-SUPPORT-BOUNDARY-*` fixtures. They are not registered in `tests/fixtures/manifest.json`, do not satisfy canonical `support:boundary` coverage, and make no canonical-promotion claim.

This bundle freezes no SupportProfile and supplies no SupportProfile object. It does not implement or exercise `checkSupport`, decide support for an actual input, validate a construction, define a ToleranceProfile, provide measured or scientific evidence, or evaluate M0F GO. Every termination-model selection remains null, so the bundle establishes no termination guarantee.

## Saved control and detector scope

`control-support-profile-candidate-catalog.json` is a hash-bound accepted control. Its stable JSON is fixed equal to `m0f/profiles/support-profile-v1.candidates.json` at generator authoring time. Verification parses the saved control bytes, uses that saved document as the source-difference anchor, and independently requires exact deterministic regeneration from the in-code control.

The control remains a candidate catalog: its catalog status is `candidate`, all `profileHash` values are null, all selections—including every termination-model selection—are null, and all evidence is pending. Acceptance therefore says only that the catalog satisfies the current candidate-catalog parser; it supplies no termination guarantee.

## Fixed negative cases

The closed ordered case list is:

- `NEG-SUPPORT-CATALOG-PREMATURE-FROZEN`
- `NEG-SUPPORT-CATALOG-PREMATURE-PROFILE-HASH`
- `NEG-SUPPORT-CATALOG-SELECT-TREE-MAX-DEGREE`
- `NEG-SUPPORT-CATALOG-SELECT-TREE-DECIMAL-DIGITS`
- `NEG-SUPPORT-CATALOG-SELECT-TREE-LENGTH-RATIO`
- `NEG-SUPPORT-CATALOG-SELECT-GRID-COLUMNS`
- `NEG-SUPPORT-CATALOG-SELECT-GRID-QUANTIZATION-ERROR`
- `NEG-SUPPORT-CATALOG-SELECT-FOLD-MAX-VERTICES`
- `NEG-SUPPORT-CATALOG-PREMATURE-EVIDENCE`
- `NEG-SUPPORT-CATALOG-METHOD-MISMATCH`
- `NEG-SUPPORT-CATALOG-UNKNOWN-SOLVER-KEY`
- `NEG-SUPPORT-CATALOG-UNSORTED-CANDIDATES`

Each case fixes the complete sorted control-to-source difference paths and the complete ordered parser issue oracle. Selection cases cover representative tree-method, box-pleating, and fold-verification candidate fields without promoting any selected value to a support boundary.

## Closed ledger and byte contract

`neg-support-catalog-candidate-bundle-ledger-v1.schema.json` is Draft 2020-12. Both the strict schema and runtime parser fix:

- twelve case rows in order, including control link, mutation, complete changed paths, source identity, and complete ordered issue oracle;
- fourteen payload artifact rows in order: README, one accepted control, and twelve negative sources;
- artifact identity, role, path, media type, lowercase SHA-256, MIT license, and exact control dependency;
- all candidate-only and explicit false-claim fields.

The directory has exactly fifteen regular files after including the ledger. Writers refuse unexpected entries instead of deleting them. Verification rejects unreadable, linked, or extra entries; hash drift; an invalid saved control; source/control delta drift; parser-oracle drift; canonical registration; and regenerated byte drift.

Serialization is stable JSON UTF-8 with one LF. This supports deterministic local parser replay; it does not provide signatures or TOCTOU protection.

## Commands

```text
npx tsx m0f/neg-support-catalog-candidate-bundle-cli.ts --verify
npx tsx m0f/neg-support-catalog-candidate-bundle-cli.ts --write
npx tsx m0f/neg-support-catalog-candidate-bundle-cli.ts --verify --json
```
