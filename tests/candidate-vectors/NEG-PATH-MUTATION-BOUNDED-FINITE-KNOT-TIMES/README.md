# NEG-PATH-MUTATION-BOUNDED-FINITE-KNOT-TIMES exact-negative candidate vector

This closed directory contains one saved project-authored single-segment bounded-interpolation FOLD control and one exact-negative mutation for the current `parseArtifactContractV1` finite-knot-time boundary. The accepted owned control declares representation version 1, status `candidate`, knot times `[0,0.5,1]`, one valid `e-hinge` angle row `[0,pi/2,pi]`, and matching interval bounds `[[0,pi/2],[pi/2,pi]]`. The mutation appends JSON `null` only at knot-time index 3. It fixes the sole `non-finite-number` issue with message `must be a finite binary64 number` at that leaf and no secondary issue. The three valid knots remain, so angle/knot and bound/knot-interval cardinalities, strict monotonicity, and endpoint coverage remain satisfied.

The bundle is intentionally outside `tests/fixtures/manifest.json`; `NEG-PATH-MUTATION-BOUNDED-FINITE-KNOT-TIMES` is not canonically registered or promoted. Exact-negative means only that the current artifact-contract parser rejects these saved bytes with the complete ordered issue signature fixed in the ledger.

This replay establishes only rejection of the saved non-finite bounded-interpolation knot-time value. It does not establish representation-version, representation-status, or supported-representation-kind handling, bounded-interpolation angle- or interval-bound-row uniqueness, angle/knot or bound/knot-interval cardinality, motion- or crease-map completeness, angle containment, strict knot ordering, path-time coverage, endpoint or physical continuity, representation selection or completeness, polynomial semantics, a radian convention, physical angle schedules or bounds, conservative bounds, kinematic feasibility, rigidity, face isometry, hinge geometry, contact analysis, CCD, collision detection or freedom, foldability, certificate-hash verification, cryptographic authenticity, canonical path-mutation-family completeness, SupportProfile, ToleranceProfile, scientific verification, or M0F GO. Local SHA-256 rows detect saved-byte drift only and are not signatures.

Verify with `npx tsx m0f/neg-path-bounded-finite-knot-times-candidate-bundle-cli.ts --verify`. Regenerate with `npx tsx m0f/neg-path-bounded-finite-knot-times-candidate-bundle-cli.ts --write`.
