# NEG-ORDER-REVERSAL exact-negative candidate vector

This closed directory contains one saved project-authored FOLD artifact-contract control and one exact-negative mutation for the current `parseArtifactContractV1` declared continuous-contact layer-order check. The mutation extends the saved terminal coplanar-overlap contact to `[0,1]` and appends the reverse face relation at `[0,0]`, while the original direction remains at `[1,1]`.

The bundle is intentionally outside `tests/fixtures/manifest.json`; `NEG-ORDER-REVERSAL` is not canonically registered or promoted. Exact-negative means only that the current artifact-contract parser rejects the saved bytes with the complete ordered issue signature fixed in the ledger.

This parser replay does not establish a physical layer order, infer contacts, establish contact completeness or legality, prove path continuity or rigid motion, perform CCD or collision detection, establish collision freedom or foldability, define a SupportProfile or ToleranceProfile, make a scientific verification claim, or evaluate M0F GO.

Verify with `npx tsx m0f/neg-order-reversal-candidate-bundle-cli.ts --verify`. Regenerate with `npx tsx m0f/neg-order-reversal-candidate-bundle-cli.ts --write`.
